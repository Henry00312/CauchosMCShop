# URL base de la API que consume el frontend.
# El código actual añade `/api` a esta variable. Para Nginx en mismo origen,
# dejar vacía esta variable; asignar `/api` produciría la ruta `/api/api`.
# Para una API externa, usar solo origen (por ejemplo, protocolo y host), sin `/api` final.
VUE_APP_API_URL=
