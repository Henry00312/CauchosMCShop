# Variables que Docker Compose usa para inicializar servicios de datos en producción.
# Copiar fuera de Git como .env.prod.local o inyectar desde el entorno de despliegue.
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=
RABBITMQ_DEFAULT_USER=
RABBITMQ_DEFAULT_PASS=
